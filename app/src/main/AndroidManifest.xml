<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.astrohackathon"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/muzyflow"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MusicLoginActivity"
            android:label="@string/app_name"
            android:uiOptions="none">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MusicLyricsActivity"/>
        <activity android:name=".MusicContributorActivity"/>
        <activity android:name=",MusicPlayerActivity"/>
        <activity android:name=".MusicRecorderActivity"/>
        <activity android:name=".ItemPlayerActivity"/>
        <activity android:name=".ItemPlayerAddActivity" />
        <activity android:name=".MainActivity" />


    </application>


</manifest>
